import { TodaysWeather } from "../../hooks/useWeather/types";

interface CurrentDayWeatherViewProps {
    todaysWeather: TodaysWeather;
    weatherCondition: string;
}
export type { CurrentDayWeatherViewProps }
